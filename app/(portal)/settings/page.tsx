export default function SettingsPage() {
  return (
    <main style={{ maxWidth: 640, margin: "0 auto", padding: 24 }}>
      <h1>Einstellungen</h1>
      <p style={{ color: "#555" }}>
        Profil, TTS-Token-Pool und Abrechnung — demnächst. API-Schlüssel findest du unter Secrets.
      </p>
    </main>
  );
}
